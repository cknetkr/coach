# Performance Coach Components

Planned component split for later `selfq` integration:

- `PerformanceCoachLayout`
- `CoachHero`
- `SubjectCardGrid`
- `GuidePanelGrid`
- `StepTabBar`
- `RubricSidebar`
- `AiFeedbackPanel`

Current static prototype still lives in the root `test/` folder.
This directory is the future React component home.
